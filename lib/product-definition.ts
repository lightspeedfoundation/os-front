/**
 * Single canonical “entity definition” for metadata, JSON-LD SoftwareApplication, and on-page copy.
 * Keep the one-line and extended paragraph aligned when editing.
 */
export const PRODUCT_DEFINITION_ONE_LINE =
  "Speed OS is a Chrome extension that turns your new tab into an onchain trading workspace: swap, bridge, and trade from the browser with an encrypted on-device vault and the same tools as Speed CLI, without moving funds to a centralized exchange.";

/** Slightly longer hero-adjacent copy; still citable. */
export const PRODUCT_DEFINITION_SHORT =
  "Speed OS is a Chrome extension that replaces the new tab with a full trading workspace. You can swap, bridge, and use onchain tools from the browser, with keys in an encrypted vault on your device and optional MCP for API environment—no CEX account required to use the in-browser flows.";
